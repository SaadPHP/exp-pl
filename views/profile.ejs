<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home Page</title>
    <style>
        h1{ 
            font-size: 32px;
            font-weight: normal;
            color: indigo;
        }

        h4{
            font-size: 20px;
            font-weight: normal;
        }

        body{
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
            background-color: lightgray;
            padding: 10px;
        }

        p{
            font-size: 20px;
            color: black;
        }
        .hobby-list{
            display: flex;
            flex-direction: row;
        }
        ul li{
            display: flex;
            flex-direction: row;
            float: left;
            margin-top: -15px;
        }
        .clear{
            clear: both;
        }
    </style>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.1/css/all.min.css" integrity="sha512-+4zCK9k+qNFUR5X+cKL9EIR+ZOhtIloNl9GIKS57V1MyNsYpYcUrUeQc9vNfzsWfV28IaLL3i96P9sdNyeRssA==" crossorigin="anonymous" />
</head>
<body>
    <h1>Welcome to profile of <%= name %></h1>
    <h4><em>I'm <%= data.age %> years old and I'm currently working as a <%= data.job %></em></h4>
    <hr/>
    <div class="hobby-list">
        <span>Hobbies :</span>
        <ul>
            <% data.hobbies.forEach( (hobby) => { %>
                <% if( hobby === 'Gaming' ){ %>
                    <li><i class="fas fa-gamepad"></i>&nbsp;<%= hobby %> |</li>  
                <% }else if( hobby === 'Programming'){ %>
                    <li>&nbsp;<i class="fas fa-code"></i>&nbsp;<%= hobby %> |</li>
                <% }else{ %>
                    <li>&nbsp;<i class="fas fa-user-ninja"></i>&nbsp;<%= hobby %> |</li>
                <% } %>
            <% }); %>
        </ul>
    </div>
    <div class="clear"></div>
    <p>This site is under maintenance [ Powered By Node & Express ].</p>
</body>
</html>